# gpu-coordinator

GPU provisioning and per-host job queueing for a local desktop, shared SSH
boxes (no sudo), and ephemeral RunPod pods. See `docs/ARCHITECTURE.md` for
the design contract and `docs/requirements-review.md` for the reasoning.
